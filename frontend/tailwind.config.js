/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // In Tailwind v4, theme customization is done in CSS using @theme directive
  // This config file is kept for content paths and future compatibility
}